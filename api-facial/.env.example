# Comma-separated API keys for client authentication
API_KEYS=key_mas_taekwondo_2026,key_otro_cliente_2026

# Optional: euclidean distance threshold (default 0.6 — lower = stricter match)
# FACE_MATCH_THRESHOLD=0.6

# Optional: allowed CORS origins
# ALLOWED_ORIGINS=https://mastkd.com
